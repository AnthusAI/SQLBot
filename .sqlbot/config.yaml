# SQLBot Configuration
# Profile to use for dbt connections
profile: Datastace

# LLM Configuration
llm:
  model: gpt-5
  max_tokens: 50000
  verbosity: low
  effort: minimal
  timeout: 90
  retries: 3

# Safety settings
dangerous: false  # Set to true to disable safeguards
